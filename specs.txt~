Meant to store contents like -
    text
    images (specially)
    misc files

> get_url(key) --------->       Master       ---------> returns url of data
                         looks up for key
                         in its lookup table

> get_data(key) ---------> Master     ----------> slave  \
  app    <----------  Master <----- (data) <--------------|

  * app and Master are usually the same

> set(data,mime)
    finds optimal node to store to, passes on the data, slave node stores it
    and gives back the id

On master

[ key | slave | id ]

On slave

PoorData
    [ data, mime ]                --> has id

PoorConfig
    [ whoismydaddy ]        --> for who ma bell tolls



MASTER -

> set (key,data,mime)
    returns True/False

 